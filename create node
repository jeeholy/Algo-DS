#include<iostream>
#include<cstdlib>
using namespace std;

struct node
{
	int info;
	struct node *next;
};

main()
{
	struct node *ptr = (struct node*) malloc(sizeof(struct node));
	cin >> ptr->info;
	ptr->next = NULL;
	cout << ptr->info;	
}
